---
# instalation: pip3 install --user yamllint
# usage: yamllint -c ~/.config/yamllint/config ~/.config/yamllint/config
extends: default


rules:
  # 80 chars should be enough, but don't fail if a line is longer
  line-length: disable
